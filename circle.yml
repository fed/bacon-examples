machine:
  node:
    version: 8.9.1

deployment:
  production:
    branch: master
    commands:
      - npm run build
      - cp dist/index.html dist/200.html
      - surge --project ./dist --domain https://bacon.surge.sh
